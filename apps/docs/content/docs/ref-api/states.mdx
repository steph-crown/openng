---
title: States
description: All states and the Federal Capital Territory
---

## List

`GET /v1/ref/states` — optional `?geo_zone=` filter.

## Detail

`GET /v1/ref/states/:slug` — e.g. `lagos`.

Use `id` and `slug` from responses to join resource rows that expose `state_id`.
