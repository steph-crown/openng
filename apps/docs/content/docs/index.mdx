---
title: What is OpenNG?
description: Nigerian public data as a versioned REST API for developers worldwide
icon: Sparkles
---

import {
  BookOpen,
  Braces,
  Calendar,
  Fuel,
  Gauge,
  Map,
  Network,
  PackageOpen,
  Utensils,
} from "lucide-react";

## Mission

OpenNG collects, validates, and publishes **Nigerian public data**—fuel, holidays, geography, and more—as **stable JSON** behind a single API surface. The goal is to replace brittle PDF scraping with something you can call from any stack and trust in production.

## Who uses OpenNG

### Product builders

#### Dashboards and internal tools

Teams wire OpenNG into **BI tools**, internal portals, and ops dashboards where fresh numbers beat static spreadsheets.

#### Research and journalism

Analysts and reporters use filters and exports to compare periods, states, and categories without maintaining one-off scrapers.

### Platform operators

If you run civic infrastructure, OpenNG is meant to sit beside your own services: **cache aggressively**, respect rate limits, and fall back to documented error codes when something drifts.

## How data reaches you

### Ingest

Imports land in **staging** with batch identifiers. Scrapers and manual pipelines write rows there first—never straight to the public tables the API reads.

### Validate

Stored procedures score each batch: required fields, referential integrity against `ref` tables, and domain rules (for example valid holiday calendars).

### Serve

Validated rows migrate to **`public`** schema tables. The API reads **only** from `public` and `ref`, so what you see in `/v1` matches what passed validation.

## What to read first

Start with [**Getting started**](/getting-started) for base URL, keys, and auth, then skim [**API formats**](/api-formats) and [**API conventions**](/api-conventions) before you open dataset pages.

<Cards className="grid gap-4 sm:grid-cols-2">
  <Card
    description="Base URL, keys, authentication, terminology, FAQ, and next links in one place."
    href="/getting-started"
    icon={<BookOpen />}
    title="Getting started"
  />
  <Card
    description="Anonymous limits vs authenticated limits; headers on every response."
    href="/rate-limits"
    icon={<Gauge />}
    title="Rate limits"
  />
  <Card
    description="Success envelope, list `meta`, and structured errors."
    href="/api-formats"
    icon={<Braces />}
    title="API formats"
  />
  <Card
    description="Headings, examples, and cross-links."
    href="/how-to-read-docs"
    icon={<BookOpen />}
    title="How to read these docs"
  />
</Cards>

## Dataset snapshot

Live and planned resources follow the same **ResourceFactory** contract: list, `meta`, detail, and export where policy allows.

<Cards className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <Card
    description="National and regional entries with year, category, and schedule metadata."
    href="/data-api/holidays"
    icon={<Calendar />}
    title="Holidays"
  />
  <Card
    description="PMS, AGO, and kerosene—monthly cadence when launched."
    href="/data-api/fuel-prices"
    icon={<Fuel />}
    title="Fuel prices"
  />
  <Card
    description="Commodity trends aligned with official statistics releases."
    href="/data-api/food-prices"
    icon={<Utensils />}
    title="Food prices"
  />
  <Card
    description="All states and FCT for joins on `state_id`."
    href="/ref-api/states"
    icon={<Map />}
    title="States"
  />
  <Card
    description="LGAs per state; required `state` filter on the list route."
    href="/ref-api/lgas"
    icon={<Map />}
    title="LGAs"
  />
  <Card
    description="`GET /health` and `GET /meta` for probes and discovery."
    href="/getting-started#health"
    icon={<Gauge />}
    title="Health & discovery"
  />
  <Card
    description="Optional bearer token for higher throughput than anonymous IP limits."
    href="/getting-started#create-an-api-key"
    icon={<Gauge />}
    title="Authenticated access"
  />
  <Card
    description="Filters, pagination, and error codes shared by every resource."
    href="/api-conventions"
    icon={<Network />}
    title="Conventions"
  />
  <Card
    description="How to propose fixes and new datasets."
    href="/contributing"
    icon={<PackageOpen />}
    title="Contribute"
  />
</Cards>

<Callout title="Keep reading">

The **On this page** column lists headings on this page. For detail on keys and auth, use [**Getting started**](/getting-started). For reading conventions, see [**How to read these docs**](/how-to-read-docs).

</Callout>
